[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "proteus"
version = "0.1.0"
description = "Local, privacy-first document-format converter (CLI + Windows right-click menu)"
license = "MIT"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "typer>=0.15",
    "rich>=13",
    "pydantic>=2",
    "pdf2docx>=0.5",
    "pymupdf>=1.24",
]

[[project.authors]]
name = "Ralf090102"
email = "ralf.hernandez090102@gmail.com"

[project.scripts]
proteus = "proteus.cli:main"

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "ruff>=0.6",
]

[dependency-groups]
dev = [
    "pytest>=8",
    "ruff>=0.6",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP"]

[tool.pytest.ini_options]
markers = [
    "integration: requires real Pandoc/LibreOffice on PATH (opt-in, `pytest -m integration`)",
]
addopts = "-m 'not integration'"
testpaths = ["tests"]

[tool.setuptools.packages.find]
where = ["src"]
